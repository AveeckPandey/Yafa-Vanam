# Frontend API types

The Go commerce API is the contract owner. Generate TypeScript API types/clients from `apps/api/openapi/openapi.yaml` into `generated/` rather than maintaining duplicate business models by hand.
