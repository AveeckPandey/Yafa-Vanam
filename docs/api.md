# Go Commerce API

The main backend lives in `apps/api` and is now Go. Public/customer endpoints, authenticated account endpoints, admin operations, payment webhooks, WhatsApp webhooks, CRM sync, merchandising, and lifecycle marketing will all route through this service.

`apps/api/openapi/openapi.yaml` is the API contract source for future generated Next.js TypeScript clients.
