# Retool

Retool is the internal admin/operations surface. It can combine PostgreSQL, HubSpot, GA4, and PostHog-derived data, while sensitive writes go through authenticated Go admin endpoints.

See `/retool` for query scaffolds and resource-connection guidance.
