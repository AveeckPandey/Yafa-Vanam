"use client";

import { useEffect, useId, useRef, useState } from "react";

type Props = { open: boolean; onClose: () => void; onLogin: (email: string, password: string, remember: boolean) => Promise<void>; onRegister: (name: string, email: string, password: string, remember: boolean) => Promise<void>; onRequestPasswordReset: (email: string) => Promise<string>; googleUrl: string; cognito?: boolean; googleEnabled?: boolean };
type Mode = "signin" | "signup" | "forgot";

function passwordStrength(password: string) { let score = 0; if (password.length >= 8) score++; if (/[a-z]/.test(password) && /[A-Z]/.test(password)) score++; if (/\d/.test(password)) score++; if (/[^A-Za-z0-9]/.test(password)) score++; return score <= 1 ? "weak" : score <= 3 ? "moderate" : "strong"; }
function Eye({ visible }: { visible: boolean }) { return <svg aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="M2.5 12s3.3-5.5 9.5-5.5S21.5 12 21.5 12 18.2 17.5 12 17.5 2.5 12 2.5 12Z" stroke="currentColor" strokeWidth="1.6"/><circle cx="12" cy="12" r="2.7" stroke="currentColor" strokeWidth="1.6"/>{!visible && <path d="m4 4 16 16" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"/>}</svg>; }
function PasswordField({ id, name, label, value, onChange, autoComplete, describedBy }: { id: string; name: string; label: string; value?: string; onChange?: (value: string) => void; autoComplete: string; describedBy?: string }) { const [visible, setVisible] = useState(false); return <label htmlFor={id}>{label}<span className="auth-modal__password-wrap"><input id={id} name={name} type={visible ? "text" : "password"} minLength={8} autoComplete={autoComplete} value={value} onChange={onChange ? (event) => onChange(event.target.value) : undefined} required aria-describedby={describedBy}/><button className="auth-modal__password-toggle" type="button" onClick={() => setVisible((current) => !current)} aria-label={`${visible ? "Hide" : "Show"} ${label.toLowerCase()}`} aria-pressed={visible}><Eye visible={visible}/></button></span></label>; }
function LeafMark() { return <svg className="auth-modal__leaf" aria-hidden="true" viewBox="0 0 108 96" fill="none"><path d="M16 81C41 72 65 47 85 12c2 30-8 58-31 72-12 7-25 6-38-3Z" stroke="currentColor" strokeWidth="1.25"/><path d="M23 77C40 62 58 44 79 22M48 59c-7-4-12-8-15-13M58 48c7 0 13-2 18-7" stroke="currentColor" strokeWidth="1.25" strokeLinecap="round"/></svg>; }

export default function AuthModal({ open, onClose, onLogin, onRegister, onRequestPasswordReset, googleUrl, cognito = false, googleEnabled = true }: Props) {
  const [mode, setMode] = useState<Mode>("signin"), [busy, setBusy] = useState(false), [error, setError] = useState(""), [notice, setNotice] = useState(""), [password, setPassword] = useState("");
  const dialog = useRef<HTMLElement>(null); const id = useId(); const errorID = `${id}-error`; const tabID = (tab: "signin" | "signup") => `${id}-${tab}-tab`; const panelID = `${id}-panel`;
  useEffect(() => { if (!open) return; const key = (event: KeyboardEvent) => event.key === "Escape" && onClose(); document.addEventListener("keydown", key); document.body.style.overflow = "hidden"; dialog.current?.focus(); return () => { document.removeEventListener("keydown", key); document.body.style.overflow = ""; }; }, [open, onClose]);
  if (!open) return null;
  const switchMode = (next: Mode) => { setMode(next); setError(""); setNotice(""); setPassword(""); };
  const submit = async (form: HTMLFormElement) => { const data = new FormData(form); const nextPassword = String(data.get("password") || ""); if (mode === "signup" && nextPassword !== String(data.get("confirmPassword") || "")) { setError("Passwords do not match. Please check and try again."); return; } setBusy(true); setError(""); setNotice(""); try { if (mode === "signin") await onLogin(String(data.get("email") || ""), nextPassword, data.get("remember") === "on"); else if (mode === "signup") await onRegister(String(data.get("name") || ""), String(data.get("email") || ""), nextPassword, data.get("remember") === "on"); else setNotice(await onRequestPasswordReset(String(data.get("email") || ""))); } catch (reason) { setError(reason instanceof Error ? reason.message : "We could not complete that request. Please try again."); } finally { setBusy(false); } };
  const strength = passwordStrength(password); const title = mode === "signin" ? "Welcome back" : mode === "signup" ? "Create your account" : "Reset your password"; const intro = cognito ? "You’ll continue securely with YAFA VANAM’s sign-in service. Your bag stays saved while you do." : mode === "signin" ? "Sign in to continue securely to checkout. Your bag is already saved." : mode === "signup" ? "A more personal beauty edit begins here." : "Enter your email and we’ll send a secure, one-time reset link if an account matches it.";
  return <div className="auth-modal__backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><section className="auth-modal" ref={dialog} role="dialog" aria-modal="true" aria-labelledby={`${id}-title`} tabIndex={-1}><LeafMark/><button className="auth-modal__close" type="button" onClick={onClose} aria-label="Close account dialog">×</button><p className="auth-modal__eyebrow">YOUR YAFA VANAM RITUAL</p><h2 id={`${id}-title`}>{title}</h2><p className="auth-modal__intro">{intro}</p>
    {mode !== "forgot" && <div className="auth-modal__tabs" role="tablist" aria-label="Account access"><button id={tabID("signin")} className={mode === "signin" ? "is-active" : ""} type="button" role="tab" aria-selected={mode === "signin"} aria-controls={panelID} onClick={() => switchMode("signin")}>Sign in</button><button id={tabID("signup")} className={mode === "signup" ? "is-active" : ""} type="button" role="tab" aria-selected={mode === "signup"} aria-controls={panelID} onClick={() => switchMode("signup")}>Sign up</button></div>}
    <div id={panelID} className="auth-modal__panel" role="tabpanel" aria-labelledby={mode === "forgot" ? undefined : tabID(mode)}>{cognito ? <><p className="auth-modal__notice">Continue to securely sign in, create an account, verify your email, or reset your password.</p><button className="auth-modal__submit" disabled={busy} type="button" onClick={() => void (mode === "signup" ? onRegister("", "", "", false) : mode === "forgot" ? onRequestPasswordReset("") : onLogin("", "", false))}>{mode === "signin" ? "Continue to sign in" : mode === "signup" ? "Continue to sign up" : "Continue to reset password"}</button>{mode === "signin" ? <button type="button" className="auth-modal__link auth-modal__back-link" onClick={() => switchMode("forgot")}>Forgot your password?</button> : mode === "forgot" ? <button type="button" className="auth-modal__link auth-modal__back-link" onClick={() => switchMode("signin")}>← Back to sign in</button> : null}{googleEnabled ? <><div className="auth-modal__divider"><span>OR CONTINUE WITH</span></div><a className="auth-modal__google" href={googleUrl}><span aria-hidden="true">G</span> Continue with Google</a></> : null}</> : <><form onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }} noValidate>{mode === "signup" && <label htmlFor={`${id}-name`}>Full name<input id={`${id}-name`} name="name" autoComplete="name" required/></label>}<label htmlFor={`${id}-email`}>Email address<input id={`${id}-email`} name="email" type="email" autoComplete="email" required aria-describedby={error ? errorID : undefined}/></label>{mode !== "forgot" && <PasswordField id={`${id}-password`} name="password" label="Password" value={password} onChange={setPassword} autoComplete={mode === "signin" ? "current-password" : "new-password"} describedBy={mode === "signup" || error ? `${mode === "signup" ? `${id}-strength ` : ""}${error ? errorID : ""}`.trim() : undefined}/>} {mode === "signup" && <div id={`${id}-strength`} className={`auth-modal__strength is-${strength}`} aria-live="polite"><span><i/><i/><i/></span><b>Password strength: {strength}</b></div>}{mode === "signup" && <PasswordField id={`${id}-confirm`} name="confirmPassword" label="Confirm password" autoComplete="new-password" describedBy={error ? errorID : undefined}/>} {mode === "signin" && <div className="auth-modal__options"><label className="auth-modal__remember"><input name="remember" type="checkbox"/> Remember me</label><button type="button" className="auth-modal__link" onClick={() => switchMode("forgot")}>Forgot password?</button></div>}{mode === "forgot" && <button type="button" className="auth-modal__link auth-modal__back-link" onClick={() => switchMode("signin")}>← Back to sign in</button>}{error && <p id={errorID} className="auth-modal__error" role="alert">{error}</p>}{notice && <p className="auth-modal__notice" role="status">{notice}</p>}<button className="auth-modal__submit" disabled={busy} type="submit">{busy ? "Please wait…" : mode === "signin" ? "Sign in" : mode === "signup" ? "Create account" : "Send reset link"}</button></form>{mode !== "forgot" && <><div className="auth-modal__divider"><span>OR CONTINUE WITH</span></div><a className="auth-modal__google" href={googleUrl}><span aria-hidden="true">G</span> Continue with Google</a>{mode === "signup" && <p className="auth-modal__switch">Already have an account? <button type="button" onClick={() => switchMode("signin")}>Sign in</button></p>}</>}</>}<p className="auth-modal__fineprint">Secure checkout begins after you sign in. You can continue browsing freely.</p></div>
  </section></div>;
}
